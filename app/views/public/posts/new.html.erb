<h1>新規投稿ページ</h1>

<%= form_with model: @post do |f| %>
  <h4>画像</h4>
  <%= f.file_field :post_image, accept: "image/*" %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>説明</h4>
  <%= f.text_area :body %>

  <%= f.submit '投稿' %>
<% end %>