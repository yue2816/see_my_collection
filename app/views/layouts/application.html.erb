<!DOCTYPE html>
<html>
  <head>
    <title>SeeMyCollection</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!--FontAwesome導入の記述-->
    <script src="https://kit.fontawesome.com/8f2160af5d.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light" style="background-color: #dbdbff">
        <div class="container">
          <a class="navbar-brand" href="/">
            <h2 class="text-dark">See My Collection</h2>
          </a>

          <div>
            <!--管理者ログイン後ヘッダー-->
            <% if admin_signed_in? %>

            <!--ユーザーログイン後ヘッダー-->
            <% elsif user_signed_in? %>

            <!--ログイン前ヘッダー-->
            <% else %>
            <ul class="navbar-nav ml-auto">
              <li class="nav-item">
                <%= link_to "About", homes_about_path, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "新規登録", new_user_registration_path, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "ログイン", new_user_session_path, class: 'nav-link text-dark' %>
              </li>
            </ul>
            <% end %>
          </div>
        </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
